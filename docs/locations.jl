# The documented example locations, shared by docs/make_locations.jl (which
# renders their figures and writes their pages) and docs/make.jl (which builds
# the navigation). Add a location here and both stay in sync.

const DOC_LOCATIONS = [
    (place = "Berlin, Germany",        slug = "berlin_germany"),
    (place = "Madrid, Spain",          slug = "madrid_spain"),
    (place = "Athens, Greece",         slug = "athens_greece"),
    (place = "Fort Collins, Colorado", slug = "fort_collins_colorado"),
]
