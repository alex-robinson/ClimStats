var documenterSearchIndex = {"docs":
[{"category":"section","location":"cities/athens_greece/#Athens,-Greece","page":"Athens, Greece","text":"Temperature climatology for Athens, Greece — from climate_day_comparison, climate_monthly and climate_daily. History is NASA POWER (1981→present, keyless and quota-free); the future band is a bias-corrected CMIP6 ensemble for 2041–2050. The series ship as a committed offline fixture (see Caching), so these figures render with no network and no quota. This page is generated by docs/make_locations.jl.\n\nnote: Partial CMIP6 ensemble\nThe 2041–2050 band uses 2 of 6 projection models cached for this location; its spread will widen once the rest are fetched.\n\n(Image: Athens, Greece: today vs. day-of-year climatology)\n\n(Image: Athens, Greece: monthly seasonal cycle)\n\n(Image: Athens, Greece: daily seasonal cycle)","title":"Athens, Greece"},{"category":"section","location":"cities/berlin_germany/#Berlin,-Germany","page":"Berlin, Germany","text":"Temperature climatology for Berlin, Germany — from climate_day_comparison, climate_monthly and climate_daily. History is NASA POWER (1981→present, keyless and quota-free); the future band is a bias-corrected CMIP6 ensemble for 2041–2050. The series ship as a committed offline fixture (see Caching), so these figures render with no network and no quota. This page is generated by docs/make_locations.jl.\n\n(Image: Berlin, Germany: today vs. day-of-year climatology)\n\n(Image: Berlin, Germany: monthly seasonal cycle)\n\n(Image: Berlin, Germany: daily seasonal cycle)","title":"Berlin, Germany"},{"category":"section","location":"cities/fort_collins_colorado/#Fort-Collins,-Colorado","page":"Fort Collins, Colorado","text":"Temperature climatology for Fort Collins, Colorado — from climate_day_comparison, climate_monthly and climate_daily. History is NASA POWER (1981→present, keyless and quota-free); the future band is a bias-corrected CMIP6 ensemble for 2041–2050. The series ship as a committed offline fixture (see Caching), so these figures render with no network and no quota. This page is generated by docs/make_locations.jl.\n\nnote: Figures pending\nThe CMIP6 projection ensemble for this location has not been fetched yet (it was capped by the Open-Meteo hourly quota during fixture generation), so the climatology figures — which need the future band — are not shown. The NASA POWER history is available: history_daily(\"Fort Collins, Colorado\").","title":"Fort Collins, Colorado"},{"category":"section","location":"climatology/#Temperature-climatology","page":"Climatology","text":"How warm is it for the time of year, and how is that shifting? ClimStats reduces the daily record to climatological normals — averages over a reference period, per calendar day and per month — and compares the present (today's forecast, the current year) and the future (a bias-corrected CMIP6 ensemble) against them.\n\nThe reference period is 1981–2010 by default, with 2011–2025 as a recent comparison window and 2041–2050 drawn from the projection ensemble.","title":"Temperature climatology"},{"category":"section","location":"climatology/#The-figures","page":"Climatology","text":"The general script examples/climatology.jl renders all three figures for any location passed as an argument (defaulting to Berlin). Run it from the package root with a Makie backend and internet access:\n\njulia --project=docs examples/climatology.jl \"Berlin, Germany\"","title":"The figures"},{"category":"section","location":"climatology/#Today-vs.-the-climatology","page":"Climatology","text":"climate_day_comparison puts today's forecast next to the day-of-year normals: a black bar for today's forecast tmin–tmax, a grey bar for the 1981–2010 reference, then 2011–2025 observed and 2041–2050 from the bias-corrected CMIP6 ensemble. Each bar spans the climatological tmin–tmax (smoothed over a ±3-day window) with a tick at the mean.\n\nusing ClimStats, CairoMakie\n\nfig = climate_day_comparison(\"Berlin, Germany\")\n\n(Image: Berlin: today vs. day-of-year climatology)","title":"Today vs. the climatology"},{"category":"section","location":"climatology/#Monthly-seasonal-cycle","page":"Climatology","text":"climate_monthly draws every observed year as a faint grey line, highlights the most recent complete year and the current (partial) year, and shades the 2041–2050 ensemble band.\n\nfig = climate_monthly(\"Berlin, Germany\")\n\n(Image: Berlin: monthly seasonal cycle)","title":"Monthly seasonal cycle"},{"category":"section","location":"climatology/#Daily-seasonal-cycle","page":"Climatology","text":"climate_daily is the daily-resolution companion: a light band for the 1981–2010 central 95 % interval, a darker 2041–2050 ensemble band, the last and current year as darker lines, and (online) the live forecast. Pass spaghetti = true to overlay every year individually.\n\nfig = climate_daily(\"Berlin, Germany\"; spaghetti = true)\n\n(Image: Berlin: daily seasonal cycle)","title":"Daily seasonal cycle"},{"category":"section","location":"climatology/#Offline-rendering","page":"Climatology","text":"Every figure can be drawn from cached data alone — no network, with the live forecast omitted — by passing offline = true or setting the CLIMSTATS_OFFLINE environment variable. A location must have been fetched online at least once so its data (and geocoding) are on disk. The four bundled example locations (Berlin, Madrid, Athens, Fort Collins) come with a committed fixture cache, so they render offline out of the box — no prior fetch needed.\n\nfig = climate_daily(\"Berlin, Germany\"; offline = true)   # cached data only\n\nClimStats.set_offline!(true)    # process-wide; returns the previous setting","title":"Offline rendering"},{"category":"section","location":"climatology/#Computing-normals-directly","page":"Climatology","text":"The figures are built on three tidy reducers; each returns a DataFrame.\n\ndata = era5_daily(\"Berlin, Germany\"; start = Date(1981, 1, 1))\n\ndaily_climatology(data; var = :tmean, window = 3, period = (1981, 2010))  # per day-of-year\nmonthly_climatology(data; var = :tmean, period = (1981, 2010))            # per calendar month\nmonthly_means(data; var = :tmean)                                         # per (year, month)","title":"Computing normals directly"},{"category":"section","location":"climatology/#API","page":"Climatology","text":"","title":"API"},{"category":"function","location":"climatology/#ClimStats.daily_climatology","page":"Climatology","text":"daily_climatology(data; var = :tmean, window = 3, period = nothing) -> DataFrame\n\nClimatological average of var for each day of the year. For every day-of-year d (1…366) the mean is taken over every observation whose date falls within ±window days of d — wrapping across the year boundary — across all years in period, an inclusive (first_year, last_year) tuple (or nothing for the whole record). The window smooths the otherwise noisy single-day average. Columns: :doy, :mean, :n (observations contributing to that day).\n\ndaily_climatology(data; var = :tmax, period = (1981, 2010))   # 30-yr normal\n\n\n\n\n\n","title":"ClimStats.daily_climatology"},{"category":"function","location":"climatology/#ClimStats.monthly_climatology","page":"Climatology","text":"monthly_climatology(data; var = :tmean, period = nothing) -> DataFrame\n\nClimatological average of var for each calendar month (1…12), pooled over all days in all years of period (an inclusive (first_year, last_year) tuple, or nothing for the whole record). Columns: :month, :mean, :n.\n\n\n\n\n\n","title":"ClimStats.monthly_climatology"},{"category":"function","location":"climatology/#ClimStats.monthly_means","page":"Climatology","text":"monthly_means(data; var = :tmean) -> DataFrame\n\nMean of var for every (year, month) in the record — the per-year monthly means that monthly_climatology averages over. Columns: :year, :month, :mean, :n_days. Useful for plotting each year's seasonal cycle on its own.\n\n\n\n\n\n","title":"ClimStats.monthly_means"},{"category":"function","location":"climatology/#ClimStats.climate_day_comparison","page":"Climatology","text":"climate_day_comparison(place; date = today(), window = 3, ref, recent, future, ...) -> Figure\n\nCompare today's forecast against the day-of-year temperature climatology across several eras, as a column of vertical bars. Each bar spans the climatological tmin–tmax for date's day-of-year (smoothed over a ±window-day window), with a tick at the mean (tmean):\n\nblack — today's forecast tmin–tmax (from forecast_daily),\ngrey  — the ref reference period (default 1981–2010),\nrecent — the recent observed period (default 2011–2025), from ERA5,\nfuture — one bar per future period (default 2041–2050), from a bias-corrected CMIP6 ensemble; silently omitted if no projection data is available.\n\nPass offline = true (or set the CLIMSTATS_OFFLINE environment variable) to plot only from cached data without any network access; the live forecast bar is omitted in that case.\n\nfig = climate_day_comparison(\"Berlin, Germany\")\nsave(\"berlin_today_vs_climate.png\", fig)\n\n\n\n\n\nclimate_day_comparison for an already-resolved Location (no geocoding).\n\n\n\n\n\n","title":"ClimStats.climate_day_comparison"},{"category":"function","location":"climatology/#ClimStats.climate_monthly","page":"Climatology","text":"climate_monthly(place; var = :tmean, hist_start, future, ...) -> Figure\n\nSeasonal cycle of monthly-mean var: every observed year drawn as a faint grey line, with the most recent complete year and the current (partial) year highlighted. When projection data is available, each future period is added as a shaded across-model band. Pass offline = true (or set CLIMSTATS_OFFLINE) to plot only from cached data without any network access.\n\nfig = climate_monthly(\"Berlin, Germany\")\nsave(\"berlin_monthly_climatology.png\", fig)\n\n\n\n\n\nclimate_monthly for an already-resolved Location (no geocoding).\n\n\n\n\n\n","title":"ClimStats.climate_monthly"},{"category":"function","location":"climatology/#ClimStats.climate_daily","page":"Climatology","text":"climate_daily(place; var = :tmean, window = 3, ref, future, spaghetti = false, ...) -> Figure\n\nDaily-resolution companion to climate_monthly: the seasonal cycle of daily var across the year (day-of-year axis), styled after the Copernicus Climate Pulse charts. Drawn back-to-front:\n\nwith spaghetti = true (the default), every available year as a faint light-grey line — the \"spaghetti\" backdrop,\na magenta band per future period from the bias-corrected CMIP6 ensemble (omitted if no projection data is available),\nthe ref-period climatological mean as a dashed grey line (1991–2020 by default, smoothed over a ±window-day window),\nthe recent_years most recent years as warm accent lines (gold → coral → burgundy, current year last), with a marker and label at the current year's latest value,\nand, when forecast = true, the live forecast mean line.\n\nPass offline = true (or set the CLIMSTATS_OFFLINE environment variable) to plot only from cached data without any network access; the live forecast is omitted in that case.\n\nfig = climate_daily(\"Berlin, Germany\"; spaghetti = true)\nsave(\"berlin_daily_climatology.png\", fig)\n\n\n\n\n\nclimate_daily for an already-resolved Location (no geocoding).\n\n\n\n\n\n","title":"ClimStats.climate_daily"},{"category":"function","location":"climatology/#ClimStats.forecast_daily","page":"Climatology","text":"forecast_daily(place; days = 7, vars, timezone) -> ClimateData\nforecast_daily(location::Location; days, vars, timezone) -> ClimateData\n\nDaily weather forecast for a location from the Open-Meteo forecast API, today through days - 1 days ahead. Same column convention as era5_daily (:date plus the requested :tmax/:tmin/:tmean/:precip), so the index and plotting helpers apply unchanged. The source label is \"forecast\".\n\nUnlike ERA5 and CMIP6 this is a small, fast-changing live request and is not cached on disk — each call fetches fresh, at the exact location (no grid snap).\n\nfc = forecast_daily(\"Berlin, Germany\"; days = 7)\nfc.table[fc.table.date .== Dates.today(), :]   # today's forecast row\n\n\n\n\n\n","title":"ClimStats.forecast_daily"},{"category":"function","location":"climatology/#ClimStats.set_offline!","page":"Climatology","text":"set_offline!(b::Bool) -> Bool\n\nEnable or disable offline mode (no network access; cached data only) and return the previous setting. Offline mode can also be turned on at load time by setting the CLIMSTATS_OFFLINE environment variable to 1/true.\n\n\n\n\n\n","title":"ClimStats.set_offline!"},{"category":"function","location":"climatology/#ClimStats.offline_mode","page":"Climatology","text":"Whether ClimStats is currently in offline mode (see set_offline!).\n\n\n\n\n\n","title":"ClimStats.offline_mode"},{"category":"section","location":"caching/#Caching","page":"Caching","text":"Downloading is the slow, rate-limited part of working with point climate data, so ClimStats caches both what it fetches and what it derives. Caching is on by default, transparent, and persists across sessions: the second time you ask for a location, it comes from disk.\n\nIt is built on DataManifest.jl's content-addressed @cached store. Each cache entry is keyed by the SHA-256 of the meaning of a call (not by a filename) and stored as a portable Arrow file, so the cache can even be shared with a Python or JavaScript front-end.","title":"Caching"},{"category":"section","location":"caching/#What-is-cached","page":"Caching","text":"Layer Function(s) Keyed by\nERA5 point series era5_daily snapped cell + stable month\nCMIP6 projection series projection_daily snapped cell + model\nNEX-GDDP point series nexgddp_daily snapped cell + model/scenario/variant/grid/variable\nBias-corrected series bias_correct content of both inputs + fit parameters\nNowcast analog completions complete_current_year, estimate_current_year content of data + analog settings","title":"What is cached"},{"category":"section","location":"caching/#Why-it-reuses-rather-than-re-fetches","page":"Caching","text":"Three design choices turn the cache from a per-request scratchpad into a genuine \"fetch once\" store:\n\nCoordinates snap to the 0.25° source grid. Two nearby queries — or the same place geocoded twice with floating-point noise — resolve to the same grid cell and therefore share one cached download.\nOne download serves many queries. era5_daily caches the full history [1950 … last complete month] once per cell (all four variables); any later call, at any date range or variable subset, is sliced from it. Only the recent, still-changing tail is fetched live each time. The stable cache key rolls forward once a month — which is also when finalised ERA5 replaces the preliminary ERA5T values at the edge, so the cache self-heals rather than freezing stale data. NEX-GDDP is a fixed archive, so its full 1950–2100 span is cached once per cell and never re-read.\nDerived results skip recompute, not just re-download. The expensive, index-independent steps — the quantile-mapping fit in bias_correct and the analog resampling behind the nowcast — are cached as their intermediates. Running ten different indices over one location recomputes nothing; only the cheap index application runs live. This is why the nowcast caches the completed analog members (and their weights) rather than any single index's estimate.","title":"Why it reuses rather than re-fetches"},{"category":"section","location":"caching/#Controlling-the-cache","page":"Caching","text":"Every cached function takes cache = true by default. Pass cache = false to bypass the cache and force a fresh computation:\n\nera5_daily(\"Berlin, Germany\"; cache = false)              # re-download\nbias_correct(model, hist; method = :qdm, cache = false)   # re-fit\nestimate_current_year(data, d -> days_above(d, 30); cache = false)\n\nEntries live under the per-project user cache directory:\n\nmacOS: ~/Library/Caches/datamanifest/projects/ClimStats/cached/\nLinux: $XDG_CACHE_HOME/datamanifest/projects/ClimStats/cached/ (default ~/.cache/...)\n\nEach entry directory holds the data.arrow payload alongside a config.toml recording the key that produced it. Deleting a directory simply forces that one result to be recomputed on next use.\n\nFor a deployed dashboard, point DataManifest's datacache_dir at a shared, persistent volume so every instance reuses a single cache.","title":"Controlling the cache"},{"category":"section","location":"caching/#API","page":"Caching","text":"","title":"API"},{"category":"function","location":"caching/#ClimStats.era5_daily","page":"Caching","text":"era5_daily(place; kwargs...) -> ClimateData\nera5_daily(location::Location; start, stop, vars, timezone) -> ClimateData\n\nDownload daily ERA5 reanalysis for a single location. A place string is geocoded automatically:\n\ndata = era5_daily(\"Berlin, Germany\")\ndata = era5_daily(\"Berlin, Germany\"; start = Date(1980,1,1), stop = Date(2024,12,31))\n\nKeyword arguments\n\nstart::Date    : first day (default 1950-01-01; ERA5 reaches back to 1940).\nstop::Date     : last day (default ≈ today − 7 days).\nvars           : variables to return (default all of (:tmax, :tmin, :tmean, :precip)).\ntimezone       : timezone for daily aggregation (default \"auto\", i.e. the location's local time, so \"days\" align with the local calendar).\ncache::Bool    : reuse an on-disk copy of the series instead of re-downloading (default true); pass false to force a fresh fetch.\n\nData are ERA5/ERA5-Land served by the Open-Meteo archive API.\n\nCaching (see src/cache.jl): the request point is snapped to the 0.25° ERA5 grid and the stable history is stored once per cell as Arrow, so any later call for the same cell — at any date range or variable subset — is served from disk. Only the recent, still-changing tail is fetched live each time; the stable cache rolls forward once a month, which is also when newly-finalised ERA5 values replace the preliminary ERA5T edge.\n\n\n\n\n\n","title":"ClimStats.era5_daily"},{"category":"function","location":"caching/#ClimStats.projection_daily","page":"Caching","text":"projection_daily(place; kwargs...) -> ClimateData\nprojection_daily(location::Location; model, start, stop, vars, timezone, cache)\n\nDownload daily CMIP6 climate-projection data for a single location (1950–2050). The full fixed span is cached on disk per model and grid cell (pass cache = false to re-download), so repeated calls are served locally. Same return type and column convention as era5_daily, so all of the index and plotting helpers apply directly.\n\nproj = projection_daily(\"Berlin, Germany\"; model = \"MRI_AGCM3_2_S\")\n\nSee PROJECTION_MODELS for available model names.\n\n\n\n\n\n","title":"ClimStats.projection_daily"},{"category":"function","location":"caching/#ClimStats.nexgddp_daily","page":"Caching","text":"nexgddp_daily(place; scenario = :ssp245, model = \"ACCESS-CM2\", kwargs...) -> ClimateData\n\nDownload a daily NEX-GDDP-CMIP6 point time series for place (a string or Location), splicing historical and the chosen SSP into one continuous series. Returns the same ClimateData shape as era5_daily, so every index, bias-correction and plotting helper works on it unchanged.\n\nKeyword arguments\n\nscenario : one of (:ssp126, :ssp245, :ssp370, :ssp585).\nmodel    : a NEX-GDDP model name (see NEXGDDP_MODELS).\nvariant / grid : override the realisation/grid labels if needed.\nbackend  : :aws (keyless S3 Open Data mirror, default) or :nccs (NASA NCCS OPeNDAP). The data is identical, so a cell cached from one backend is reused by the other.\nstart / stop   : date range (default 1950-01-01 … 2100-12-31).\nvars     : variables to fetch (default all of (:tmax, :tmin, :tmean, :precip)).\n\nnote: Note\nThis requires the NetCDF stack. Run using NCDatasets once to enable it (NCDatasets is an optional dependency, loaded via a package extension).\n\n\n\n\n\n","title":"ClimStats.nexgddp_daily"},{"category":"function","location":"caching/#ClimStats.bias_correct","page":"Caching","text":"bias_correct(model, obs; method = :delta, cache = true, kwargs...) -> ClimateData\n\nFit a correction of model towards obs and apply it in one step. method and kwargs are forwarded to fit_bias_correction. The corrected series is cached on disk keyed by the content of both inputs and the fit parameters; pass cache = false to recompute.\n\n\n\n\n\nbias_correct(ens::Ensemble, obs::ClimateData; kwargs...) -> Ensemble\n\nBias-correct every member of an ensemble against the observed baseline obs (see bias_correct). kwargs are forwarded to fit_bias_correction.\n\n\n\n\n\n","title":"ClimStats.bias_correct"},{"category":"section","location":"","page":"Home","text":"<p align=\"center\">\n  <img src=\"assets/logo.svg\" alt=\"ClimStats logo\" width=\"160\" height=\"160\">\n</p>","title":"Home"},{"category":"section","location":"#ClimStats","page":"Home","text":"A Julia package for retrieving climate data for a single location and computing indices from it — ERA5 reanalysis for the past and CMIP6 projections for the future. Write a place name, get back the data and a figure.\n\nusing ClimStats\nusing CairoMakie        # a Makie backend (CairoMakie for files, GLMakie for windows)\n\nfig = climate_timeseries(\"Berlin, Germany\"; threshold = 30)\nsave(\"berlin_hot_days.png\", fig)\n\nHistorical data default to NASA POWER (keyless, no request quota); pass source = :era5 for the Open-Meteo ERA5 archive instead. The CMIP6 ensemble is fetched from the key-less Open-Meteo climate API. Everything is cached on disk on first use so the same series is never re-downloaded (see Caching), and the example locations ship with a bundled offline fixture cache. See the README for the full tour — indices, bias correction, ensembles and SSP scenarios. These docs focus on caching, the temperature climatology figures, and the current-year nowcast.","title":"ClimStats"},{"category":"section","location":"#Contents","page":"Home","text":"Pages = [\"caching.md\", \"climatology.md\", \"nowcast.md\"]\nDepth = 2","title":"Contents"},{"category":"section","location":"nowcast/#Current-year-nowcast","page":"Current-year nowcast","text":"ERA5 lags real time by about a week, so the current calendar year is always incomplete. An index computed on it is misleading: a hot-day count or a precipitation total is only a lower bound (the summer may not have happened yet), and an annual mean is biased toward whichever season has elapsed.\n\nRather than plot that partial value, ClimStats completes the year from weighted analog years and shows a statistical estimate with an uncertainty band — a lighter diamond with a lo–hi error bar, distinct from the solid observed history.","title":"Current-year nowcast"},{"category":"section","location":"nowcast/#The-two-figures","page":"Current-year nowcast","text":"The general script examples/nowcast.jl renders both figures below for any location (defaulting to Berlin). Run it from the package root with a Makie backend and internet access:\n\njulia --project=docs examples/nowcast.jl \"Berlin, Germany\"","title":"The two figures"},{"category":"section","location":"nowcast/#History-current-year-estimate","page":"Current-year nowcast","text":"climate_timeseries drops the trailing partial year from the solid ERA5 line and overlays the nowcast estimate. The x-axis is fixed to 1950–2050 so it lines up with the projection figure.\n\nusing ClimStats, CairoMakie, Dates\n\nfig = climate_timeseries(\"Berlin, Germany\"; threshold = 30,\n                         start = Date(1950, 1, 1))   # nowcast = true by default\n\n(Image: Berlin hot days with current-year estimate)","title":"History + current-year estimate"},{"category":"section","location":"nowcast/#History-estimate-CMIP6-projection","page":"Current-year nowcast","text":"The same estimate appears on the combined past+future figure: ERA5 history, the 2026 nowcast, and a bias-corrected CMIP6 ensemble (median line + shaded spread) out to 2050.\n\nfig = climate_projection(\"Berlin, Germany\"; threshold = 30,\n                         hist_start = Date(1950, 1, 1),\n                         proj_stop = Date(2050, 12, 31))\n\nnote: Figure\nRun examples/nowcast.jl to render this combined past+future figure (it needs live Open-Meteo access for the CMIP6 ensemble, so it is not embedded here).\n\nPass nowcast = false to either helper to fall back to the raw partial value.","title":"History + estimate + CMIP6 projection"},{"category":"section","location":"nowcast/#How-the-estimate-is-built","page":"Current-year nowcast","text":"Analog resampling, in src/nowcast.jl:\n\nSimilarity — compare the current year's observed window (Jan 1 → last available day) against the same calendar window of every complete prior year, by RMSE of the daily similarity variable. Closer years are better analogs.\nWeights — turn those distances into weights with a Gaussian kernel, optionally keeping only the top-K analogs.\nAnchored trajectories — for each analog year, take its remaining days as a candidate trajectory for the rest of this year, shifted so its window mean matches the current year's (additive for temperatures, multiplicative for precipitation). This tracks how the year is actually running and preserves the day-to-day structure within an analog.\nIndex distribution — each anchored analog yields a completed daily series; running an index over the weighted members gives the median and lo–hi band that are plotted.\n\nThe uncertainty therefore lives at the index level (derived from the ensemble), while daily provenance lives in the data: each completed member carries a Boolean :estimated column (false for observed days, true for analog-filled days), so the synthetic tail never silently mixes with observations.","title":"How the estimate is built"},{"category":"section","location":"nowcast/#Calling-the-estimator-directly","page":"Current-year nowcast","text":"data = era5_daily(\"Berlin, Germany\"; start = Date(1950, 1, 1))\n\nest = estimate_current_year(data, d -> days_above(d, 30; var = :tmax); var = :tmax)\n# CurrentYearEstimate(2026: 5 [1–11], 165/365 days, 20 analogs)  (illustrative)\n\nest.median, est.lo, est.hi      # the estimate and its band\nest.observed_partial            # the (lower-bound) count from observed days only\n\nmembers = complete_current_year(data; var = :tmax)   # the completed daily series\nmembers[1].table.estimated                            # false for observed, true for filled","title":"Calling the estimator directly"},{"category":"section","location":"nowcast/#API","page":"Current-year nowcast","text":"","title":"API"},{"category":"function","location":"nowcast/#ClimStats.estimate_current_year","page":"Current-year nowcast","text":"estimate_current_year(data, indexfn; var, valuecol, lo = 0.1, hi = 0.9, cache = true, kwargs...) -> CurrentYearEstimate\n\nEstimate an annual index for the trailing, incomplete year of data by running indexfn (a ClimateData -> DataFrame index, e.g. d -> days_above(d, 30)) over the weighted analog completions from complete_current_year and summarising the result as a weighted median with a lo–hi band.\n\nvar selects the variable the analog similarity keys on (default the index's own variable when called from the high-level helpers); valuecol the index column to summarise (auto-detected by default). Extra keyword arguments (n_analogs, bandwidth, min_obs, maxscale) tune the analog builder and cache toggles reuse of the (index-independent) completions. Returns a CurrentYearEstimate.\n\n\n\n\n\n","title":"ClimStats.estimate_current_year"},{"category":"function","location":"nowcast/#ClimStats.complete_current_year","page":"Current-year nowcast","text":"complete_current_year(data; var, n_analogs = 20, bandwidth, min_obs, maxscale, cache = true) -> Vector{ClimateData}\n\nBuild the ensemble of completed daily series for the trailing, incomplete year of data: one ClimateData per weighted analog year, each the observed history plus the year's remaining days filled from that analog and anchored to how the current year is running. Every member carries a Boolean :estimated column (false for observed days, true for filled days).\n\nvar (default mean temperature if present) drives the analog similarity; see the module notes for the method. The completions are cached on disk keyed by the content of data and the analog settings (cache = false recomputes). Errors if the final year is already complete.\n\n\n\n\n\n","title":"ClimStats.complete_current_year"},{"category":"type","location":"nowcast/#ClimStats.CurrentYearEstimate","page":"Current-year nowcast","text":"CurrentYearEstimate\n\nStatistical estimate of an annual index for the trailing, incomplete year, produced by estimate_current_year from weighted analog completions.\n\nFields\n\nyear             : the incomplete calendar year.\nn_obs/n_total  : observed days so far / days in the full year.\nobserved_partial : the index computed on the observed days only (for counts and sums this is a lower bound; shown for reference, not plotted).\nmedian,lo,hi : weighted median and lo–hi quantiles of the index across the completed analog members — the estimate and its uncertainty band.\nmembers          : per-analog (; year, value, weight) — the full weighted distribution behind the summary.\n\n\n\n\n\n","title":"ClimStats.CurrentYearEstimate"},{"category":"function","location":"nowcast/#ClimStats.incomplete_final_year","page":"Current-year nowcast","text":"incomplete_final_year(data) -> Union{Int,Nothing}\n\nThe last calendar year of data if it does not reach 31 December (and so is a partial year worth nowcasting), or nothing if the final year is complete.\n\n\n\n\n\n","title":"ClimStats.incomplete_final_year"},{"category":"function","location":"nowcast/#ClimStats.plot_nowcast!","page":"Current-year nowcast","text":"plot_nowcast!(target, est; color = 1, label, markersize = 9, kwargs...) -> target\n\nOverlay a CurrentYearEstimate for the incomplete final year onto target (an Axis or Figure): a lighter-shade diamond marker at the weighted median with a vertical lo–hi error bar, so it reads as an estimate distinct from the solid observed history. Returns target.\n\n\n\n\n\n","title":"ClimStats.plot_nowcast!"},{"category":"section","location":"cities/madrid_spain/#Madrid,-Spain","page":"Madrid, Spain","text":"Temperature climatology for Madrid, Spain — from climate_day_comparison, climate_monthly and climate_daily. History is NASA POWER (1981→present, keyless and quota-free); the future band is a bias-corrected CMIP6 ensemble for 2041–2050. The series ship as a committed offline fixture (see Caching), so these figures render with no network and no quota. This page is generated by docs/make_locations.jl.\n\n(Image: Madrid, Spain: today vs. day-of-year climatology)\n\n(Image: Madrid, Spain: monthly seasonal cycle)\n\n(Image: Madrid, Spain: daily seasonal cycle)","title":"Madrid, Spain"}]
}
